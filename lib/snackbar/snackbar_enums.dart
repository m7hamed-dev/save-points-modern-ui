/// Snackbar type enum
enum SnackbarType {
  info,
  success,
  error,
  warning,
}

/// Snackbar position enum
enum SnackbarPosition {
  top,
  bottom,
}

/// Snackbar animation style enum
enum SnackbarAnimation {
  fadeSlide,
  scale,
  slide,
  bounce,
  rotate,
  elastic,
  slideRotate,
  none,
}

