name: example
description: Example app demonstrating save_points_snackbar_dialog_bottomsheet package
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  save_points_snackbar_dialog_bottomsheet:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

